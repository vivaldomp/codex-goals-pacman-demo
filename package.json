{
  "name": "codex-goal-pacman-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "pack-sprites": "node scripts/pack-sprites.mjs",
    "optimize-assets": "node scripts/optimize-assets.mjs"
  },
  "dependencies": {
    "vite": "^5.4.21"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^1.6.1"
  }
}
