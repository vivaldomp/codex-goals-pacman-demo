# Runtime Assets

Runtime bitmap files are generated by `npm run pack-sprites` into `/assets` and copied to `/public/assets` for Vite. The source code keeps this directory as the module-owned asset boundary while the generated PNG files remain in the project-level asset pipeline required by the plan.
